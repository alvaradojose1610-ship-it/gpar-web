/** Tamaño máximo del archivo que sube el usuario (antes de comprimir). */
export const MAX_IMAGEN_PRODUCTO_BYTES = 1.5 * 1024 * 1024;

/** Ancho máx. de la imagen de ficha / catálogo (WebP). */
export const ANCHO_IMAGEN_PRODUCTO = 1200;

/** Ancho máx. del thumb para listados del panel / grillas. */
export const ANCHO_THUMB_PRODUCTO = 320;

export const TAG_CATALOGO_PUBLICO = "catalogo-publico";
